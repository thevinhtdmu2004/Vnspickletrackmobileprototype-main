# Screens Foundation

This folder is reserved for production screen-level composition.

Sprint 00 baseline:

- Existing prototype screens remain under `src/app/components`.
- Future production screens may be introduced here when a sprint explicitly extracts or rebuilds screen composition.
- UI labels should remain Vietnamese.
- Preserve the validated Admin, Coach and Member flows.

Do not migrate existing screens during Sprint 00.
